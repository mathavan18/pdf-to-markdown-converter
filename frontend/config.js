// Local/default config. The GitHub Pages deploy workflow overwrites this
// file at deploy time with the production backend URL (see
// .github/workflows/deploy-pages.yml and the API_BASE_URL repo variable).
window.PDF_TO_MD_API_BASE = window.PDF_TO_MD_API_BASE || "http://localhost:8000";
